# Rust build artifacts
/target/
**/*.rs.bk

# Datasets: downloaded, never committed (see scripts/download_datasets.sh)
# Under WSL2 the real location is $ANKA_DATASETS on ext4 — see docs/DESIGN.md.
/datasets

# Index artifacts
*.anka
*.anka.tmp
wal.log

# Python tooling for dataset conversion and the hnswlib baseline
.venv/
__pycache__/
*.pyc

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Internal Turkish working spec — not part of the published repository.
# The repo-facing design document is docs/DESIGN.md.
ANKA_PROJE_SPEC.md

# Assistant working notes, kept out of the repository for the same reason as the spec above.
# Delete this line to publish it.
CLAUDE.md
