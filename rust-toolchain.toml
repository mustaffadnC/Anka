[toolchain]
# Pinned for reproducibility (see docs/DESIGN.md, "Reproducibility contract").
# Keep in sync with the `toolchain:` input in .github/workflows/ci.yml.
channel = "1.97.1"
components = ["rustfmt", "clippy"]
